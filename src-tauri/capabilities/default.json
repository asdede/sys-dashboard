{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for every widget window. core:default covers the IPC bridge that handles `invoke()` calls plus the window-drag operation used by the [data-tauri-drag-region] attribute. Each widget is its own top-level window so we must list them all explicitly.",
  "windows": ["cpu", "ram", "gpu", "forecast"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging"
  ]
}
